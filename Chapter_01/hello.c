#include <stdio.h>

int main(void)
{
    //using `puts` when printing strings
    // can also use `printf("%s", "string");
    puts("Hello, world!!!");
    printf("%s", "Welcome to C\n");
}

/*
use : gcc -Wall -Wextra -std=c2x <file>.c -o <file>
*/
